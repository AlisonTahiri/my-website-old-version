export type Language = {
  id: number;
  src: string;
  name: string;
  alt: string;
  stars: number;
};

export type ProjectsData = {
  url: string;
  content: string;
  logoAlt: string;
  src: string;
  logoWidth: string;
  tags: string[];
};
